# Dependencies
node_modules/
.pnp
.pnp.js

# Environment
.env
.env.local
.env.*.local

# Build outputs
dist/
build/
.next/
out/

# Capacitor — the ios/ and android/ folders ARE committed because they
# contain customizations (Info.plist, icons, splash, signing config).
# Only the generated/local-only bits inside them are ignored.
app/ios/App/Pods/
app/ios/App/Podfile.lock
app/ios/.gradle/
app/android/.gradle/
app/android/local.properties
app/android/.idea/
app/android/app/build/
app/android/build/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.vscode/
.idea/

# Supabase local
supabase/.branches/
supabase/.temp/
